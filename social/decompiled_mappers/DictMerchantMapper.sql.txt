SELECT * FROM dict_merchant WHERE contact_phone = #{phone} LIMIT 1
SELECT * FROM dict_merchant WHERE REPLACE(merchant_name, \' \', \'\') LIKE CONCAT(\'%\', REPLACE(#{keyword}, \' \', \'\'), \'%\') OR REPLACE(merchant_short_name, \' \', \'\') LIKE CONCAT(\'%\', REPLACE(#{keyword}, \' \', \'\'), \'%\') GROUP BY merchant_id ORDER BY   CASE WHEN REPLACE(merchant_name, \' \', \'\') = REPLACE(#{keyword}, \' \', \'\') THEN 0        WHEN REPLACE(merchant_short_name, \' \', \'\') = REPLACE(#{keyword}, \' \', \'\') THEN 1        WHEN REPLACE(merchant_name, \' \', \'\') LIKE CONCAT(REPLACE(#{keyword}, \' \', \'\'), \'%\') THEN 2        WHEN REPLACE(merchant_short_name, \' \', \'\') LIKE CONCAT(REPLACE(#{keyword}, \' \', \'\'), \'%\') THEN 3        ELSE 4 END,   LENGTH(REPLACE(merchant_name, \' \', \'\')) ASC LIMIT 20
SELECT * FROM dict_merchant
<script>SELECT * FROM dict_merchant WHERE contact_phone IN <foreach collection=\'phones\' item=\'phone\' open=\'(\' separator=\',\' close=\')\'>#{phone}</foreach></script>
SELECT * FROM dict_merchant WHERE REPLACE(merchant_name, \' \', \'\') = REPLACE(#{merchantName}, \' \', \'\') LIMIT 1
